# Root of the config hierarchy; no parent directory is consulted.
config.stopBubbling = true

# Marks generated members with @lombok.Generated so JaCoCo excludes them from coverage.
# Without this the build gate would measure code that was never written by hand.
lombok.addLombokGeneratedAnnotation = true
